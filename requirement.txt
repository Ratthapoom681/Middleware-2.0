# Linux host packages for this Docker Compose project.
#
# Ubuntu/Debian install:
#   sudo apt-get update
#   sudo apt-get install -y $(grep -vE '^\s*(#|$)' requirement.txt)
#
# These packages are enough to clone the repo and run:
#   docker compose up -d --build
#
# JavaScript dependencies are installed from each service's package-lock.json
# during Docker builds, or manually with npm ci inside each service directory.

ca-certificates
curl
git
docker.io
docker-compose-v2
